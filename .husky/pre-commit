#!/usr/bin/env bash

branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ] || [ "$branch" = "master" ]; then
  echo "Error: Direct commits to $branch are not allowed. Please use a feature branch."
  exit 1
fi

npx lint-staged
npm run type-check
